dev.test:
  run:
    - cargo build
    - cargo test

dev:
  release:
    git:
      depends:
        - dev.test
      run:
        - git add -A
        - git commit -S
        - git push

    snap:
      run:
        - snapcraft